# Squishees

Style refs: `ref-jelly.jpeg` (jellyfish), `ref-berry.jpeg` (strawberry). Generated toys are `*.png` (magenta keyed). Poke clips: `frog-poke.mp4`, `cat-poke.mp4`. Catalog: `catalog.json`.

Use PNGs in the UI. Poke = CSS squash (scaleY bounce) plus optional mp4. Locked toys are silhouettes until earned.
